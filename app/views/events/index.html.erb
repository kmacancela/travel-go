<h1> See events all over the world 🌎 </h1>

<%if logged_in?%>
    <h1>Logged in</h1>    
<%end%>
<ul>
<% @events.each do |event| %>
<li>
<%= link_to event.title, event_path(event) %>
</li>
<% end %>
</ul>
